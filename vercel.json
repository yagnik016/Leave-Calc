{
  "buildCommand": "cd FE && npm install && npm run build",
  "outputDirectory": "FE/.next",
  "installCommand": "npm install --legacy-peer-deps && cd BE && npm install --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/BE/serverless.js"
    }
  ]
}
